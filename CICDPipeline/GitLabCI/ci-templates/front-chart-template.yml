apiVersion: v2
name: frontend-${name}
description: An umbrella chart for the frontend-${name} application
type: application
version: 0.1.0
dependencies:
- name: webapp-base
  version: 0.1.0
  repository: "file://../../../base/webapp-base"
  alias: web
  condition: web.enabled
